#include <iostream>
#include <algorithm>

using namespace std;

int partition_set(int arr[], int n) {
        
}

int main() {
    int arr[] = { 1, 6, 11, 5 };
    int n = sizeof(arr) / sizeof(arr[0]);
    cout << partition_set(arr, n);
    return 0;
}